import "./App.css";

const App = () => (
  <div className="card">
    <button onClick={() => {}}>count is lol broke</button>
    <p>
      Edit <code>src/App.tsx</code> and save to test HMR
    </p>
  </div>
);

export default App;
